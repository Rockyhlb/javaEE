<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace 声明实现的是哪个接口 -->
<mapper namespace="com.hlb.mybatis.mapper.UserInfoXmlMapper">
    <resultMap id="BaseResult" type="com.hlb.mybatis.model.UserInfo">
        <!-- 主键 -->
        <id property="id" column="id"></id>
        <!-- 普通列 -->
        <result column="username" property="username"></result>
        <result column="password" property="password"></result>
        <result column="age" property="age"></result>
        <result column="gender" property="gender"></result>
        <result column="phone" property="phone"></result>
        <result column="delete_flag" property="deleteFlag"></result>
        <result column="create_time" property="createTime"></result>
        <result column="update_time" property="updateTime"></result>
    </resultMap>

    <select id="queryAllUser" resultMap="BaseResult">
        select * from user_info
    </select>

    <select id="queryAllUser1" resultMap="BaseResult">
        select id,username,password,age,gender,phone,
               delete_flag, create_time,update_time
        from user_info;
    </select>

    <select id="queryUserByParam" resultMap="BaseResult">
        select * from user_info where id = #{id} and delete_flag = #{deleteFlag}
    </select>

    <!-- 设置useGeneratedKeys 和keyProperty属性,返回自增ID-->
    <insert id="insertByOb" useGeneratedKeys="true" keyProperty="id">
        insert into user_info(username,password,age,gender,phone)
        values (#{user.username},#{user.password},#{user.age},
                #{user.gender},#{user.phone})
    </insert>

    <delete id="deleteByUserId">
        delete from user_info where id = #{userId}
    </delete>

    <update id="updateByUserId">
        update user_info set password=#{password},update_time=#{updateTime}
                         where id=#{id}
    </update>

    <update id="updateByUserOb">
        update user_info set username=#{user.username},password=#{user.password},
                             update_time=#{user.updateTime} where id=#{user.id}
    </update>
</mapper>